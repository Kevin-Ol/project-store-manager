const validateProducts = require('./validateProducts');
const decrementProducts = require('./decrementProducts');
const incrementProducts = require('./incrementProducts');

module.exports = {
  validateProducts,
  decrementProducts,
  incrementProducts,
};
